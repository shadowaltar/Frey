<UserControl x:Class="Trading.StrategyBuilder.Views.RuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Width="140"
             Height="140"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource SmallTextStyle}" TargetType="TextBlock" />
        <Style BasedOn="{StaticResource TextBoxStyle}" TargetType="TextBox" />
        <Style BasedOn="{StaticResource LabelTextStyle}" TargetType="Label" />
        <Style BasedOn="{StaticResource DropdownTextStyle}" TargetType="ComboBox" />
    </UserControl.Resources>
    <Grid Width="135" Height="135" Margin="0,0,5,5"
             Background="Orange">
        <TextBlock Margin="10,10,0,0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Text="RULE" />
        <TextBlock x:Name="RuleIndex"
                   Margin="42,10,0,0"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Top"
                   Text="00" />
        <ListBox x:Name="Rules" Margin="10,31,15,15" />
    </Grid>
</UserControl>
